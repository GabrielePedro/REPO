{
  "name": "materiais-site",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --watch styles --watch *.html --ext html,css,js",
    "check:styles": "./scripts/check-inline-styles.sh",
    "install-githooks": "sh ./scripts/install-git-hook.sh || true"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
